import { makeStyles } from '@mui/material/styles';

const useStyles = makeStyles(() => ({
  ListIcon: {
    color: '#000',
    fontSize: '5rem',
  },

  ListText: {
    color: '#000',
    fontSize: '5rem',
  },
}));

export default useStyles;
